<p id="notice"><%= notice %></p>

<h1>Editar Comuna</h1>

<%= form_with(model: @comuna, method: :patch) do |form| %>
    <div class="field">
        <%= form.label :nombre %>
        <%= form.text_field :nombre %>
    </div>
    <div class="field">
        <%= form.label :cuarentena %>
        <%= form.select :cuarentena, options_for_select(["true", "false"]) %>
    </div>
    <div class="actions">
        <%= form.submit %>
    </div>
<% end %>

<br>

<%= link_to 'Lista Comunas', lista_comunas_path %>