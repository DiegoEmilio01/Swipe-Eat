<% if current_admin %>
  <p id="notice">
    <% if (notice != 'Guardado') and (notice)%>
        <h2>ERROR:</h2>
        <% if notice["nombre"] %>
            <%= notice["nombre"][0] %><br>
        <% end %>
        <% if notice["descripcion"] %>
            <%= notice["descripcion"][0] %><br>
        <% end %>
    <% elsif notice == 'Guardado'%>
        <%= 'Gusto guardado exitosamente.' %><br>
    <% end %>
  </p>

  <h1>Crear gusto</h1>

  <%= form_with(model: @gustos, local: true, method: :post) do |form| %>
  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>
  <div class="field">
    <%= form.label :descripción %>
    <%= form.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
  <% end %>
  <br>
  <%= link_to "Volver a lista", gustos_path %>
<% else %>
  <% controller.redirect_to new_admin_session_path %>
<% end %>